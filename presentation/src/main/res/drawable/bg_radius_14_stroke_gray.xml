<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 배경색 (Fill = FAFAFA) -->
    <solid android:color="#FAFAFA" />

    <!-- 둥근 모서리 -->
    <corners android:radius="14dp" />

    <!-- 테두리 색상 (Stroke = EAEBEC) -->
    <stroke
        android:width="1dp"
        android:color="#EAEBEC" />

</shape>
