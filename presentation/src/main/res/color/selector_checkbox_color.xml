<!-- res/color/selector_checkbox_color.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 체크된 상태 -->
    <item android:color="@color/main_color" android:state_checked="true"/>
    <!-- 기본 (체크 안됨) -->
    <item android:color="@color/gray_scale_5"/>
</selector>
